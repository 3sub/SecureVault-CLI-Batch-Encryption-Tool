# SecureVault-CLI-Batch-Encryption-Tool
A command-line companion to SecureVault: it bulk-encrypts a folder of files into the exact .vault format used by the browser app, so you can import them in one shot instead of dragging files in one by one.
